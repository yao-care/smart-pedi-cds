# SEO / AEO 基建 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 為兒童發展評估網站補齊 SEO/AEO 基建——單一真相源、結構化資料、索引邊界對齊、去全站舊定位、守門測試——讓家長能搜到衛教內容、AI 能引用本站。

**Architecture:** 品牌字串集中在 `scripts/base.mjs`（跨 build/runtime），`src/lib/seo/` 提供 schema 工廠與站點常數，`JsonLd.astro` 統一注入結構化資料；`noindex` 單一 prop 同時驅動 meta-robots 與 Pagefind 排除，sitemap 用路徑 filter；robots/llms 改 Astro endpoint 由真相源投影。

**Tech Stack:** Astro 5 SSG endpoint、Svelte 5、TypeScript strict、vitest、@astrojs/sitemap、@astrojs/rss、Pagefind、sharp（既有）；OG 圖新增 satori + @resvg/resvg-js（見前置決策）。

**Spec:** `docs/superpowers/specs/2026-05-27-seo-aeo-design.md`

---

## 前置決策（實作前確認）

**OG 圖中文字型方案**：`public/fonts` 只有 PDF 用的 Noto Sans TC **subset woff2**（base64 `.js`），opentype.js 不支援 woff2/subset，sharp 渲染 SVG 中文也不可靠。本計畫 Task 22 採 **satori + @resvg/resvg-js + 一次性下載完整 Noto Sans TC TTF**（業界 @vercel/og 同款管線）。這是全計畫唯一需新增 npm 依賴的 task。若不想新增依賴，替代方案：OG 圖只用拉丁「Smart Pedi」+ 品牌圖形（無中文），則 Task 22 改用純 sharp+SVG。**預設採 satori 方案**；執行到 Task 22 前可改。

---

## 檔案結構

**新增：**
| 檔案 | 職責 |
|---|---|
| `src/lib/seo/site.ts` | 站點常數（組織/品牌/路徑），品牌字串 import 自 `base.mjs` |
| `src/lib/seo/schema.ts` | schema.org 工廠函式（Organization/WebSite/SoftwareApplication/MedicalWebPage/ItemList/Breadcrumb/FAQPage） |
| `src/components/seo/JsonLd.astro` | 通用 JSON-LD 注入元件 |
| `src/pages/robots.txt.ts` | robots.txt endpoint |
| `src/pages/llms.txt.ts` | llms.txt endpoint（投影 education） |
| `src/pages/404.astro` | 自訂 404 |
| `src/pages/search.astro` | 站內搜尋（Pagefind UI） |
| `scripts/generate-og-image.mjs` | OG 圖生成（satori） |
| `tests/seo/schema.test.ts` | schema 工廠單元測試 |
| `tests/seo/positioning.test.ts` | 原始碼字串守門（去舊定位/網址一致） |
| `scripts/verify-seo.mjs` | 建置後 dist 守門（robots/llms/sitemap/404/h1/pagefind） |

**修改：**
| 檔案 | 改動 |
|---|---|
| `scripts/base.mjs` | 新增 `SITE_NAME`/`SITE_SHORT_NAME`/`SITE_TAGLINE`/`SITE_DESCRIPTION` |
| `src/layouts/Base.astro` | `noindex` prop、og 強化、RSS autodiscovery、author/application-name、title 後綴吃 SITE |
| `src/layouts/App.astro` | `noindex` prop 轉傳 + 條件 pagefind |
| `src/layouts/Workspace.astro` | `noindex` prop 轉傳 |
| `src/pages/index.astro` | 注入 Organization/WebSite/SoftwareApplication/FAQPage schema |
| `src/pages/about.astro` | 去舊定位改寫 + 改名「臨床決策流程」+ Organization schema + 移除 FAQPage |
| `src/pages/education/[...slug].astro` | h1 + MedicalWebPage schema + Breadcrumb 改用 schema 工廠 |
| `src/pages/education/index.astro` | ItemList schema + 常駐可見文章清單 |
| `src/pages/assess.astro` | SSR h1 + 引導文字 + noindex（否，維持索引）|
| `src/pages/history.astro`、`result/index.astro`、`admin/card-review.astro`、`settings.astro`、`about/illustration-credits.astro`、`workspace/*` | 傳 `noindex` |
| `src/data/site-faqs.ts` | 改寫家長/工具層 FAQ |
| `src/components/blocks/Footer.astro` | 吃 SITE、去 SMART on FHIR/舊站名/死連結 |
| `src/components/blocks/Header.astro` | history 移出主導覽、site-title 吃 SITE |
| `src/components/blocks/Breadcrumb.astro` | 改用 schema 工廠 + JsonLd |
| `src/pages/rss.xml.ts` | title/description 吃 SITE |
| `scripts/templates/manifest.template.json` | name/short_name/description 改佔位符 |
| `scripts/build-manifest.mjs` | 注入品牌佔位符 |
| `astro.config.mjs` | sitemap filter + serialize |
| `package.json` | postbuild 加 verify-seo；新增 generate:og |

---

## Phase 1：真相源與 schema 基建

### Task 1：品牌字串真相源（base.mjs + site.ts）

**Files:**
- Modify: `scripts/base.mjs`
- Create: `src/lib/seo/site.ts`
- Test: `tests/seo/positioning.test.ts`（Task 23 完整化，此處先建最小版）

- [ ] **Step 1：在 `scripts/base.mjs` 末尾新增品牌常數**

```js
// 品牌字串真相源（跨 build/runtime）。site.ts 匯入後組成 SITE。
export const SITE_NAME = 'Smart Pedi 兒童發展智慧評估';
export const SITE_SHORT_NAME = 'Smart Pedi';
export const SITE_TAGLINE = '給 0–6 歲家長的免費兒童發展篩檢工具';
export const SITE_DESCRIPTION = '在瀏覽器完成的兒童發展評估，依年齡評估語言、動作、社交、認知等發展里程碑，並提供對應衛教內容。結果非醫療診斷，發現疑慮建議就醫評估。';
```

- [ ] **Step 2：建立 `src/lib/seo/site.ts`**

```ts
import {
  SITE_NAME,
  SITE_SHORT_NAME,
  SITE_TAGLINE,
  SITE_DESCRIPTION,
} from '../../../scripts/base.mjs';

export const SITE = {
  name: SITE_NAME,
  shortName: SITE_SHORT_NAME,
  tagline: SITE_TAGLINE,
  description: SITE_DESCRIPTION,
  inLanguage: 'zh-TW',
  logoPath: '/icons/icon-512.png',
  ogImagePath: '/og/og-default.png',
  organization: {
    name: 'yao.care 藥提醒科技',
    legalName: 'yao.care 藥提醒科技',
    url: 'https://yao.care',
  },
  repo: 'https://github.com/yao-care/smart-pedi-cds',
  sameAs: [] as string[],
} as const;
```

- [ ] **Step 3：確認 TypeScript 能解析 `.mjs` import**

Run: `pnpm check`
Expected: 無新錯誤（`allowJs:true` 已開，`.ts` import `.mjs` 合法）。若報 implicit-any，於 `scripts/base.mjs` 旁的型別由 allowJs 推斷即可，無需 `.d.ts`。

- [ ] **Step 4：Commit**

```bash
git add scripts/base.mjs src/lib/seo/site.ts
git commit -m "feat(seo): 品牌字串真相源 base.mjs + site.ts"
```

---

### Task 2：schema 工廠 + 單元測試（TDD）

**Files:**
- Create: `src/lib/seo/schema.ts`
- Test: `tests/seo/schema.test.ts`

- [ ] **Step 1：寫失敗測試 `tests/seo/schema.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import {
  organizationSchema,
  webSiteSchema,
  softwareApplicationSchema,
  medicalWebPageSchema,
  articleListSchema,
  breadcrumbSchema,
  faqPageSchema,
} from '../../src/lib/seo/schema';

const site = new URL('https://smart-pedi-cds.yao.care/');

describe('organizationSchema', () => {
  it('帶 @type Organization 與機構名', () => {
    const s = organizationSchema(site);
    expect(s['@context']).toBe('https://schema.org');
    expect(s['@type']).toBe('Organization');
    expect(s.name).toBe('yao.care 藥提醒科技');
    expect(s.url).toBe('https://yao.care');
  });
  it('sameAs 為空時不輸出該欄位', () => {
    const s = organizationSchema(site) as Record<string, unknown>;
    expect('sameAs' in s).toBe(false);
  });
});

describe('webSiteSchema', () => {
  it('含 SearchAction，target 指向 /search', () => {
    const s = webSiteSchema(site);
    expect(s['@type']).toBe('WebSite');
    expect(s.name).toBe('Smart Pedi 兒童發展智慧評估');
    expect(s.potentialAction['@type']).toBe('SearchAction');
    expect(s.potentialAction.target).toContain('/search?q=');
  });
});

describe('softwareApplicationSchema', () => {
  it('免費 HealthApplication', () => {
    const s = softwareApplicationSchema(site);
    expect(s['@type']).toBe('SoftwareApplication');
    expect(s.applicationCategory).toBe('HealthApplication');
    expect(s.offers.price).toBe('0');
    expect(s.isAccessibleForFree).toBe(true);
  });
});

describe('medicalWebPageSchema', () => {
  const s = medicalWebPageSchema(site, {
    title: '語言發展',
    summary: '摘要',
    ageGroups: ['toddler'],
    url: 'https://smart-pedi-cds.yao.care/education/lang/',
    publishedAt: new Date('2026-01-01'),
    updatedAt: new Date('2026-02-01'),
  });
  it('MedicalWebPage + 兒科 specialty + 家長 audience', () => {
    expect(s['@type']).toBe('MedicalWebPage');
    expect(s.specialty).toBe('Pediatrics');
    expect(s.audience['@type']).toBe('MedicalAudience');
    expect(s.audience.audienceType).toBe('Parent');
  });
  it('日期：dateModified 用 updatedAt，缺則用 publishedAt', () => {
    expect(s.datePublished).toBe('2026-01-01T00:00:00.000Z');
    expect(s.dateModified).toBe('2026-02-01T00:00:00.000Z');
    const s2 = medicalWebPageSchema(site, {
      title: 't', summary: 's', ageGroups: ['infant'],
      url: 'https://x/', publishedAt: new Date('2026-01-01'),
    });
    expect(s2.dateModified).toBe('2026-01-01T00:00:00.000Z');
  });
});

describe('articleListSchema', () => {
  it('ItemList，position 從 1 起', () => {
    const s = articleListSchema(site, [
      { name: 'A', url: 'https://x/a/' },
      { name: 'B', url: 'https://x/b/' },
    ]);
    expect(s['@type']).toBe('ItemList');
    expect(s.itemListElement[0].position).toBe(1);
    expect(s.itemListElement[1].name).toBe('B');
  });
});

describe('breadcrumbSchema', () => {
  it('BreadcrumbList，含 href 才有 item', () => {
    const s = breadcrumbSchema(site, [
      { label: '首頁', href: '/' },
      { label: '當前' },
    ]);
    expect(s['@type']).toBe('BreadcrumbList');
    expect(s.itemListElement[0].item).toBe('https://smart-pedi-cds.yao.care/');
    expect('item' in s.itemListElement[1]).toBe(false);
  });
});

describe('faqPageSchema', () => {
  it('FAQPage，每題轉 Question/Answer', () => {
    const s = faqPageSchema([{ question: 'Q1', answer: 'A1' }]);
    expect(s['@type']).toBe('FAQPage');
    expect(s.mainEntity[0]['@type']).toBe('Question');
    expect(s.mainEntity[0].acceptedAnswer.text).toBe('A1');
  });
});

describe('序列化', () => {
  it('所有工廠輸出可 JSON.stringify', () => {
    expect(() => JSON.stringify([
      organizationSchema(site), webSiteSchema(site),
      softwareApplicationSchema(site),
    ])).not.toThrow();
  });
});
```

- [ ] **Step 2：跑測試確認失敗**

Run: `pnpm test -- tests/seo/schema.test.ts`
Expected: FAIL（`schema.ts` 不存在，import 解析錯誤）。

- [ ] **Step 3：實作 `src/lib/seo/schema.ts`**

```ts
import { SITE } from './site';

const CONTEXT = 'https://schema.org';
const abs = (site: URL, path: string) => new URL(path, site).href;

function organizationNode(site: URL) {
  return {
    '@type': 'Organization',
    name: SITE.organization.name,
    legalName: SITE.organization.legalName,
    url: SITE.organization.url,
    logo: abs(site, SITE.logoPath),
    ...(SITE.sameAs.length ? { sameAs: SITE.sameAs } : {}),
  };
}

export function organizationSchema(site: URL) {
  return { '@context': CONTEXT, ...organizationNode(site) };
}

export function webSiteSchema(site: URL) {
  return {
    '@context': CONTEXT,
    '@type': 'WebSite',
    name: SITE.name,
    url: abs(site, '/'),
    inLanguage: SITE.inLanguage,
    publisher: organizationNode(site),
    potentialAction: {
      '@type': 'SearchAction',
      target: `${abs(site, '/search')}?q={search_term_string}`,
      'query-input': 'required name=search_term_string',
    },
  };
}

export function softwareApplicationSchema(site: URL) {
  return {
    '@context': CONTEXT,
    '@type': 'SoftwareApplication',
    name: SITE.name,
    applicationCategory: 'HealthApplication',
    operatingSystem: 'Web',
    url: abs(site, '/'),
    inLanguage: SITE.inLanguage,
    isAccessibleForFree: true,
    offers: { '@type': 'Offer', price: '0', priceCurrency: 'TWD' },
    publisher: organizationNode(site),
  };
}

interface MedicalWebPageInput {
  title: string;
  summary: string;
  ageGroups: string[];
  url: string;
  publishedAt: Date;
  updatedAt?: Date;
}

export function medicalWebPageSchema(site: URL, input: MedicalWebPageInput) {
  const modified = (input.updatedAt ?? input.publishedAt).toISOString();
  return {
    '@context': CONTEXT,
    '@type': 'MedicalWebPage',
    name: input.title,
    description: input.summary,
    url: input.url,
    inLanguage: SITE.inLanguage,
    specialty: 'Pediatrics',
    audience: { '@type': 'MedicalAudience', audienceType: 'Parent' },
    isPartOf: { '@type': 'WebSite', name: SITE.name, url: abs(site, '/') },
    publisher: organizationNode(site),
    datePublished: input.publishedAt.toISOString(),
    dateModified: modified,
    lastReviewed: modified,
  };
}

export function articleListSchema(site: URL, articles: { name: string; url: string }[]) {
  return {
    '@context': CONTEXT,
    '@type': 'ItemList',
    itemListElement: articles.map((a, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: a.name,
      url: a.url,
    })),
  };
}

export function breadcrumbSchema(site: URL, items: { label: string; href?: string }[]) {
  return {
    '@context': CONTEXT,
    '@type': 'BreadcrumbList',
    itemListElement: items.map((item, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: item.label,
      ...(item.href ? { item: abs(site, item.href) } : {}),
    })),
  };
}

export function faqPageSchema(faqs: { question: string; answer: string }[]) {
  return {
    '@context': CONTEXT,
    '@type': 'FAQPage',
    mainEntity: faqs.map((f) => ({
      '@type': 'Question',
      name: f.question,
      acceptedAnswer: { '@type': 'Answer', text: f.answer },
    })),
  };
}
```

- [ ] **Step 4：跑測試確認通過**

Run: `pnpm test -- tests/seo/schema.test.ts`
Expected: PASS（所有斷言綠）。

- [ ] **Step 5：Commit**

```bash
git add src/lib/seo/schema.ts tests/seo/schema.test.ts
git commit -m "feat(seo): schema.org 工廠 + 單元測試"
```

---

### Task 3：JsonLd 元件 + Breadcrumb 改用

**Files:**
- Create: `src/components/seo/JsonLd.astro`
- Modify: `src/components/blocks/Breadcrumb.astro:1-35`

- [ ] **Step 1：建立 `src/components/seo/JsonLd.astro`**

```astro
---
interface Props { data: object | object[]; }
const { data } = Astro.props;
---
<script type="application/ld+json" set:html={JSON.stringify(data)} />
```

- [ ] **Step 2：改 `Breadcrumb.astro` frontmatter 用 schema 工廠**

將 frontmatter（行 1-17）的 inline `jsonLd` 物件改為：

```astro
---
import { breadcrumbSchema } from '../../lib/seo/schema';
import JsonLd from '../seo/JsonLd.astro';

interface Props {
  items: { label: string; href?: string }[];
}

const { items } = Astro.props;
const jsonLd = breadcrumbSchema(Astro.site!, items);
---
```

並將檔尾的 `<script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />`（行 35）改為：

```astro
<JsonLd data={jsonLd} />
```

（`<nav>...</nav>` 與 `<style>` 不變。）

- [ ] **Step 3：驗證 build 與型別**

Run: `pnpm check`
Expected: 無新錯誤。

- [ ] **Step 4：Commit**

```bash
git add src/components/seo/JsonLd.astro src/components/blocks/Breadcrumb.astro
git commit -m "feat(seo): JsonLd 元件 + Breadcrumb 改用 schema 工廠"
```

---

## Phase 2：Base 與索引邊界（noindex + Pagefind 對齊）

### Task 4：Base.astro — noindex / og 強化 / RSS autodiscovery / 品牌一致

**Files:**
- Modify: `src/layouts/Base.astro:1-50`

- [ ] **Step 1：改 frontmatter（行 1-19）**

```astro
---
import OfflineBanner from '../components/common/OfflineBanner.svelte';
import { BASE_PATH } from '../../scripts/base.mjs';
import { SITE } from '../lib/seo/site';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  noindex?: boolean;
}

const { title, description, canonical, noindex = false } = Astro.props;
const canonicalURL = canonical
  ? new URL(canonical, Astro.site)
  : new URL(Astro.url.pathname, Astro.site);
const fullTitle = `${title} | ${SITE.shortName}`;
const base = BASE_PATH;
const ogImage = new URL(base + SITE.ogImagePath, Astro.site).href;

import '../styles/global.css';
---
```

- [ ] **Step 2：在 `<meta name="description" .../>`（行 28）後加 noindex 條件**

```astro
<meta name="description" content={description} />
{noindex && <meta name="robots" content="noindex, nofollow" />}
```

- [ ] **Step 3：在 `<meta name="theme-color" .../>`（行 34）後加 author / application-name / RSS autodiscovery**

```astro
<meta name="theme-color" content="#3d6b54" />
<meta name="author" content={SITE.organization.name} />
<meta name="application-name" content={SITE.shortName} />
<link rel="alternate" type="application/rss+xml" title="衛教內容" href={new URL(base + '/rss.xml', Astro.site).href} />
```

- [ ] **Step 4：把 Open Graph 區塊（行 36-42）改為含 site_name 與尺寸**

```astro
<!-- Open Graph -->
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:type" content="website" />
<meta property="og:locale" content="zh_TW" />
<meta property="og:url" content={canonicalURL.href} />
<meta property="og:site_name" content={SITE.name} />
<meta property="og:image" content={ogImage} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:image:alt" content={SITE.name} />
```

- [ ] **Step 5：把 Twitter image（行 46）改為 `ogImage`**

```astro
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:image" content={ogImage} />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
```

- [ ] **Step 6：驗證**

Run: `pnpm check`
Expected: 無新錯誤。（注意：`og/og-default.png` 尚未產生，引用路徑無礙 build；圖於 Task 22 產出。）

- [ ] **Step 7：Commit**

```bash
git add src/layouts/Base.astro
git commit -m "feat(seo): Base 加 noindex/og 強化/RSS autodiscovery/品牌一致"
```

---

### Task 5：App / Workspace layout — noindex 轉傳 + Pagefind 條件

**Files:**
- Modify: `src/layouts/App.astro:6-19`
- Modify: `src/layouts/Workspace.astro:5-13`

- [ ] **Step 1：改 `App.astro` Props 與 main**

frontmatter（行 6-11）改為：

```astro
interface Props {
  title: string;
  description: string;
  noindex?: boolean;
}

const { title, description, noindex = false } = Astro.props;
```

`<Base ...>` 與 `<main ...>`（行 14-17）改為：

```astro
<Base title={title} description={description} noindex={noindex}>
  <div class="app-layout">
    <Header />
    <main id="main-content" class="container" {...(noindex ? { 'data-pagefind-ignore': true } : { 'data-pagefind-body': true })}>
```

（其餘 `<slot/>`、`<Footer/>`、`</Base>`、`<style>` 不變。）

- [ ] **Step 2：改 `Workspace.astro` Props（無 pagefind-body，僅轉傳 noindex）**

frontmatter（行 5-10）改為：

```astro
interface Props {
  title: string;
  description?: string;
  noindex?: boolean;
}

const { title, description = 'CDSS 臨床監測工作台', noindex = false } = Astro.props;
```

`<Base ...>`（行 13）改為：

```astro
<Base title={title} description={description} noindex={noindex}>
```

> 註：Workspace 的 `default description` 含 "CDSS"，但 workspace/* 一律 noindex 且不進 Pagefind，不影響對外索引；保留不改以免擴大範圍。

- [ ] **Step 3：驗證**

Run: `pnpm check`
Expected: 無新錯誤。

- [ ] **Step 4：Commit**

```bash
git add src/layouts/App.astro src/layouts/Workspace.astro
git commit -m "feat(seo): App/Workspace 轉傳 noindex + App main 條件 Pagefind"
```

---

### Task 6：對工具/隱私頁套用 noindex（含自寫 main 的 Pagefind 排除）

**Files:**
- Modify: `src/pages/history.astro:7`
- Modify: `src/pages/result/index.astro:7`
- Modify: `src/pages/admin/card-review.astro:33,35`
- Modify: `src/pages/settings.astro:26`
- Modify: `src/pages/about/illustration-credits.astro:17,19`
- Modify: `src/pages/workspace/index.astro:6`（及其他 `workspace/**/*.astro`）

- [ ] **Step 1：直接用 Base 的頁 → 加 `noindex`**

`history.astro` 行 7：
```astro
<Base title="評估紀錄" description="過去的兒童發展評估紀錄" noindex>
```
`result/index.astro` 行 7：
```astro
<Base title="評估結果" description="兒童發展智慧評估結果" noindex>
```

- [ ] **Step 2：自寫 `data-pagefind-body` 的 noindex 頁 → 加 noindex 並改 ignore**

`history.astro` 行 10 的 `<main ... data-pagefind-body>` 改為：
```astro
<main id="main-content" class="history-main" data-pagefind-ignore>
```
`admin/card-review.astro`：行 33 `<Base ...>` 加 `noindex`；行 35 `<main ... data-pagefind-body>` → `data-pagefind-ignore`。
`about/illustration-credits.astro`：行 17 `<Base ...>` 加 `noindex`；行 19 `<main ... data-pagefind-body>` → `data-pagefind-ignore`。

- [ ] **Step 3：經 App/Workspace layout 的頁 → 傳 noindex prop**

`settings.astro` 行 26：
```astro
<App title="系統設定" description="FHIR Server、規則、Webhook、通知偏好設定" noindex>
```
`workspace/index.astro` 行 6（及所有 `src/pages/workspace/**/*.astro` 的 `<Workspace ...>`）：
```astro
<Workspace title="臨床監測工作台" noindex>
```

Run: `grep -rl "<Workspace" src/pages/workspace/` 找出所有 workspace 頁，逐一加 `noindex`。

- [ ] **Step 4：驗證**

Run: `pnpm check && pnpm build`
Expected: build 成功。

- [ ] **Step 5：Commit**

```bash
git add src/pages/history.astro src/pages/result/index.astro src/pages/admin/card-review.astro src/pages/settings.astro src/pages/about/illustration-credits.astro src/pages/workspace/
git commit -m "feat(seo): 工具/隱私頁套用 noindex + Pagefind 排除"
```

---

## Phase 3：Endpoints 與 sitemap

### Task 7：robots.txt endpoint

**Files:**
- Create: `src/pages/robots.txt.ts`

- [ ] **Step 1：建立 `src/pages/robots.txt.ts`**

```ts
import type { APIContext } from 'astro';

const DISALLOW = ['/settings', '/admin', '/result', '/workspace', '/history'];

export function GET(context: APIContext) {
  const sitemap = new URL('/sitemap-index.xml', context.site!).href;
  const body = [
    'User-agent: *',
    'Allow: /',
    ...DISALLOW.map((p) => `Disallow: ${p}`),
    '',
    `Sitemap: ${sitemap}`,
    '',
  ].join('\n');
  return new Response(body, {
    headers: { 'Content-Type': 'text/plain; charset=utf-8' },
  });
}
```

> 註：`/search`、`/about/illustration-credits`、`/404` 刻意不 Disallow（靠 noindex），保留 search 運作與內部連結爬取。

- [ ] **Step 2：驗證產出**

Run: `pnpm build` 後 `cat dist/robots.txt`
Expected: 含 `Sitemap: https://smart-pedi-cds.yao.care/sitemap-index.xml`、5 條 Disallow，無 `/search`。

- [ ] **Step 3：Commit**

```bash
git add src/pages/robots.txt.ts
git commit -m "feat(seo): robots.txt endpoint（含 sitemap 與 noindex 路徑 Disallow）"
```

---

### Task 8：llms.txt endpoint（投影 education）+ 刪舊靜態檔

**Files:**
- Create: `src/pages/llms.txt.ts`
- Delete: `public/llms.txt`、`public/llms-full.txt`

- [ ] **Step 1：建立 `src/pages/llms.txt.ts`**

```ts
import type { APIContext } from 'astro';
import { getCollection } from 'astro:content';
import { SITE } from '../lib/seo/site';

export async function GET(context: APIContext) {
  const site = context.site!;
  const abs = (p: string) => new URL(p, site).href;
  const education = await getCollection('education');
  const topics = education
    .map((e) => `- [${e.data.title}](${abs(`/education/${e.id}/`)}): ${e.data.summary}`)
    .join('\n');

  const body = `# ${SITE.name}

> ${SITE.tagline}，在瀏覽器完成、不上傳個資。
> 評估語言、動作、社交、認知等發展里程碑，並提供對應衛教內容。

## 這是什麼
- 家長可自行操作的兒童發展評估，依年齡給適齡題目
- 結果非醫療診斷，發現疑慮建議就醫評估
- 由 ${SITE.organization.name} 開發，開源、純瀏覽器、零個資上傳

## 衛教主題
${topics}

## 開始評估
- [家長評估入口](${abs('/assess/')})
- [衛教內容](${abs('/education/')})
- [內容更新 RSS](${abs('/rss.xml')})

## 原始碼
${SITE.repo}
`;
  return new Response(body, {
    headers: { 'Content-Type': 'text/plain; charset=utf-8' },
  });
}
```

- [ ] **Step 2：刪除過時靜態檔**

```bash
git rm public/llms.txt public/llms-full.txt
```

- [ ] **Step 3：驗證產出**

Run: `pnpm build` 後 `cat dist/llms.txt`
Expected: 含 `# Smart Pedi 兒童發展智慧評估`、正確網域、`github.com/yao-care/...`、衛教主題逐篇列出；無 `yaocare`、無「臨床決策/生命徵象」。

- [ ] **Step 4：Commit**

```bash
git add src/pages/llms.txt.ts public/llms.txt public/llms-full.txt
git commit -m "feat(seo): llms.txt endpoint（發展評估定位 + 衛教投影），刪過時靜態檔"
```

---

### Task 9：rss.xml.ts 去舊定位

**Files:**
- Modify: `src/pages/rss.xml.ts:7-9`

- [ ] **Step 1：改 title/description 吃 SITE**

frontmatter 加 import，並改 `rss({...})` 的前兩行：

```ts
import rss from '@astrojs/rss';
import type { APIContext } from 'astro';
import { getCollection } from 'astro:content';
import { SITE } from '../lib/seo/site';

export async function GET(context: APIContext) {
  const education = await getCollection('education');
  return rss({
    title: `${SITE.name} — 衛教內容`,
    description: SITE.description,
    site: context.site!.toString(),
    items: education.map((entry) => ({
      title: entry.data.title,
      description: entry.data.summary,
      pubDate: entry.data.publishedAt,
      link: `/education/${entry.id}/`,
    })),
    customData: '<language>zh-TW</language>',
  });
}
```

- [ ] **Step 2：驗證**

Run: `pnpm build` 後 `grep -i "title" dist/rss.xml | head -1`
Expected: 含 `Smart Pedi 兒童發展智慧評估 — 衛教內容`，無「CDSS 兒科臨床決策」。

- [ ] **Step 3：Commit**

```bash
git add src/pages/rss.xml.ts
git commit -m "fix(seo): RSS 標題/描述去舊定位，吃 SITE 真相源"
```

---

### Task 10：sitemap filter（排除 noindex 頁）

**Files:**
- Modify: `astro.config.mjs:13`

- [ ] **Step 1：把 `sitemap()` 改為帶 filter**

行 13 `integrations: [svelte(), sitemap()],` 改為：

```js
integrations: [
  svelte(),
  sitemap({
    filter: (page) =>
      !/\/(settings|admin|result|workspace|history|search)(\/|$)/.test(page) &&
      !page.includes('/about/illustration-credits') &&
      !page.endsWith('/404/'),
  }),
],
```

> lastmod 增強為選用：若要對 education 頁帶 `lastmod`，可加 `serialize`，惟 `astro.config` 無法 `getCollection`，需以 `fs` + `js-yaml`（專案已裝）讀 `src/data/education/*.md` frontmatter 的 `updatedAt ?? publishedAt`。本 task 預設只做 filter（P1）；lastmod 列為後續可選。

- [ ] **Step 2：驗證**

Run: `pnpm build` 後 `grep -E "settings|workspace|history|/result" dist/sitemap-0.xml || echo "OK 無 noindex 路徑"`
Expected: `OK 無 noindex 路徑`。

- [ ] **Step 3：Commit**

```bash
git add astro.config.mjs
git commit -m "feat(seo): sitemap filter 排除 noindex 頁"
```

---

## Phase 4：頁面結構化資料與內容

### Task 11：site-faqs.ts 改寫家長/工具層 FAQ

**Files:**
- Modify: `src/data/site-faqs.ts`（整檔取代）

- [ ] **Step 1：整檔取代為家長導向 FAQ（移除 `faqJsonLd`，改由 schema 工廠產生）**

```ts
export interface Faq {
  question: string;
  answer: string;
}

export const siteFaqs: Faq[] = [
  {
    question: '這個評估是做什麼的？評估孩子哪些發展面向？',
    answer: 'Smart Pedi 是免費的兒童發展篩檢自評工具，依孩子年齡給適齡題目，評估粗動作、精細動作、語言理解、語言表達、認知、社會情緒六大發展面向，並提供對應的衛教建議。',
  },
  {
    question: '評估結果可以取代醫師診斷嗎？',
    answer: '不行。評估結果僅為發展篩檢與分流參考，不是醫療診斷，也未經信效度驗證。若您對孩子的發展有疑慮，或孩子有急性症狀，請諮詢小兒科或兒童發展相關專業人員。',
  },
  {
    question: '我和孩子的資料會被上傳嗎？',
    answer: '不會。所有運算都在您的瀏覽器內完成，資料不離開裝置、不收集、不轉傳。系統為純靜態網頁，部署後沒有後端伺服器。',
  },
  {
    question: '需要費用、登入或預約嗎？',
    answer: '都不需要。Smart Pedi 完全免費、免登入、免安裝，用現代瀏覽器（Chrome、Firefox、Safari、Edge）開啟即可使用，也支援離線開啟。',
  },
  {
    question: '評估完發現疑慮，下一步該怎麼辦？',
    answer: '評估結果會依「分級 × 需注意面向」帶出對應的衛教文章與參考影片，提供居家可做的互動建議。若結果建議進一步評估，請諮詢小兒科或兒童發展專業人員做正式評估。',
  },
];
```

- [ ] **Step 2：Commit（測試在 Task 12/13 連動，故先單獨提交資料）**

```bash
git add src/data/site-faqs.ts
git commit -m "feat(seo): FAQ 改寫為家長/工具層問答"
```

---

### Task 12：首頁注入 Organization/WebSite/SoftwareApplication/FAQPage schema

**Files:**
- Modify: `src/pages/index.astro:2-3,26`

- [ ] **Step 1：改 frontmatter import（行 2-3）**

```astro
---
import App from '../layouts/App.astro';
import { siteFaqs } from '../data/site-faqs';
import JsonLd from '../components/seo/JsonLd.astro';
import {
  organizationSchema,
  webSiteSchema,
  softwareApplicationSchema,
  faqPageSchema,
} from '../lib/seo/schema';

const site = Astro.site!;
// （以下 steps/gains/trust 陣列不變）
```

- [ ] **Step 2：把行 26 的 inline FAQ script 改為注入四個 schema**

```astro
<App title="免費兒童發展篩檢自評" description="免費的兒童發展遲緩自評工具：依年齡引導式問卷，即時提供發展風險分級與衛教建議。資料只在瀏覽器端運算，免登入、保護隱私。">
  <JsonLd data={[organizationSchema(site), webSiteSchema(site), softwareApplicationSchema(site), faqPageSchema(siteFaqs)]} />
```

（其餘 hero/sections/FAQ 顯示區/style 不變；FAQ 仍以 `siteFaqs` 渲染 `<details>`。）

- [ ] **Step 3：驗證**

Run: `pnpm check`
Expected: 無錯誤（`faqJsonLd` 已不再被 import）。

- [ ] **Step 4：Commit**

```bash
git add src/pages/index.astro
git commit -m "feat(seo): 首頁注入 Organization/WebSite/SoftwareApplication/FAQPage"
```

---

### Task 13：about.astro 去舊定位改寫 + Organization schema

**Files:**
- Modify: `src/pages/about.astro:2-10,43-67,112-122,201`

- [ ] **Step 1：改 frontmatter import 與 introHighlights（行 2-10）**

```astro
---
import App from '../layouts/App.astro';
import JsonLd from '../components/seo/JsonLd.astro';
import { organizationSchema } from '../lib/seo/schema';

const introHighlights = [
  '家長可自行操作的兒童發展篩檢',
  '隱私優先：資料不離開瀏覽器',
  '開源免費，免登入、免安裝',
  '六大發展面向，依年齡適性出題',
];
// （flowSteps / outcomes 陣列不變）
```

- [ ] **Step 2：改 `<App>` 開頭（行 43-46）：meta description、schema、h1、系統介紹正文**

```astro
<App title="關於" description="關於 Smart Pedi 兒童發展智慧評估：免費、純瀏覽器、保護隱私的兒童發展篩檢自評工具。">
  <JsonLd data={organizationSchema(Astro.site!)} />

  <h1>關於 Smart Pedi 兒童發展智慧評估</h1>

  <section class="about-section about-twocol" aria-label="系統介紹">
    <div class="twocol-main">
      <h2>系統介紹</h2>
      <p>
        Smart Pedi 是一套開源、免費的兒童發展篩檢自評工具。
        家長依孩子的年齡引導式作答，當下取得六大發展面向的風險分級與對應衛教建議。
        所有運算都在瀏覽器端完成，資料不離開裝置、不收集、不轉傳。
      </p>
    </div>
```

（`<aside>` 重點摘要區塊不變，會渲染新的 `introHighlights`。）

- [ ] **Step 3：把「臨床決策流程」改名為「評估判讀流程」（行 66-67）**

```astro
  <section class="about-section flow-section" aria-label="評估判讀流程">
    <h2>評估判讀流程</h2>
```

並把行 201 CSS 註解 `/* ---------- 臨床決策流程 ---------- */` 改為 `/* ---------- 評估判讀流程 ---------- */`。

- [ ] **Step 4：移除 FAQ section（行 112-122 整段）**

刪除：

```astro
  <section class="about-section" aria-label="常見問題">
    <h2>常見問題</h2>
    <div class="faq-list">
      {siteFaqs.map((faq) => (
        <details class="faq-item">
          <summary class="faq-question">{faq.question}</summary>
          <p class="faq-answer">{faq.answer}</p>
        </details>
      ))}
    </div>
  </section>
```

（FAQPage 只保留在首頁，去重。對應的 `.faq-*` CSS 可留著不影響，或一併刪除。）

- [ ] **Step 5：驗證去定位**

Run: `pnpm check && grep -nE "臨床決策|SMART on FHIR|CDSS|生命徵象|雙角色|監測指標|接收預警" src/pages/about.astro || echo "OK 無舊定位字串"`
Expected: `OK 無舊定位字串`（`flowSteps`/`outcomes` 內容為發展評估判讀，不含上述字串）。

- [ ] **Step 6：Commit**

```bash
git add src/pages/about.astro
git commit -m "fix(seo): about 去舊定位改寫 + Organization schema + 去重 FAQ"
```

---

### Task 14：衛教文章頁 — h1 + MedicalWebPage schema

**Files:**
- Modify: `src/pages/education/[...slug].astro:1-16,46-50`

- [ ] **Step 1：frontmatter 加 schema（行 1-16 之後）**

在現有 import 後加：

```astro
import JsonLd from '../../components/seo/JsonLd.astro';
import { medicalWebPageSchema } from '../../lib/seo/schema';
```

在 `const breadcrumbs = [...]` 之後加：

```astro
const medicalSchema = medicalWebPageSchema(Astro.site!, {
  title: entry.data.title,
  summary: entry.data.summary,
  ageGroups: entry.data.ageGroup,
  url: new URL(`/education/${entry.id}/`, Astro.site!).href,
  publishedAt: entry.data.publishedAt,
  updatedAt: entry.data.updatedAt,
});
```

- [ ] **Step 2：在 `<Education ...>` 內最前面加 JsonLd 與 h1（行 46-51）**

把：

```astro
<Education
  title={entry.data.title}
  description={entry.data.summary}
  breadcrumbs={breadcrumbs}
>
  <div class="content-meta">
```

改為：

```astro
<Education
  title={entry.data.title}
  description={entry.data.summary}
  breadcrumbs={breadcrumbs}
>
  <JsonLd data={medicalSchema} />
  <h1>{entry.data.title}</h1>

  <div class="content-meta">
```

（已驗證 19 篇文章正文皆從 `##` 起跳，無自帶 h1，無重複風險。）

- [ ] **Step 3：驗證**

Run: `pnpm build` 後 `grep -c "<h1" dist/education/*/index.html | head -3`
Expected: 每頁 ≥ 1 個 `<h1`。

- [ ] **Step 4：Commit**

```bash
git add src/pages/education/[...slug].astro
git commit -m "feat(seo): 衛教文章頁加 h1 + MedicalWebPage schema"
```

---

### Task 15：衛教列表頁 — ItemList + 常駐可見文章清單

**Files:**
- Modify: `src/pages/education/index.astro:1-9,57-58`

- [ ] **Step 1：frontmatter 加 schema 與文章清單（接在行 9 `const allEducation` 之後）**

在現有 import 後加：

```astro
import JsonLd from '../../components/seo/JsonLd.astro';
import { articleListSchema } from '../../lib/seo/schema';
```

在 `const allEducation = await getCollection('education');` 之後加：

```astro
const articleEntries = allEducation
  .map((e) => ({ slug: e.id, title: e.data.title }))
  .sort((a, b) => a.title.localeCompare(b.title, 'zh-Hant'));
const articleListJsonLd = articleListSchema(
  Astro.site!,
  articleEntries.map((a) => ({
    name: a.title,
    url: new URL(`/education/${a.slug}/`, Astro.site!).href,
  })),
);
```

- [ ] **Step 2：在 `<h1>衛教資源</h1>`（行 58）後注入 schema + 常駐清單**

```astro
<App title="衛教資源" description="兒科健康衛教資源（年齡 × 發展領域矩陣）">
  <JsonLd data={articleListJsonLd} />
  <h1>衛教資源</h1>

  <nav class="all-articles" aria-label="所有衛教文章">
    <h2>所有衛教文章</h2>
    <ul>
      {articleEntries.map((a) => (
        <li><a href={`/education/${a.slug}/`}>{a.title}</a></li>
      ))}
    </ul>
  </nav>
```

- [ ] **Step 3：在 `<style>` 內加常駐清單樣式**

```css
  .all-articles {
    margin-bottom: var(--space-6);
  }
  .all-articles h2 {
    font-size: var(--text-lg);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-3);
    color: var(--text);
  }
  .all-articles ul {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2) var(--space-4);
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .all-articles a {
    font-size: var(--text-base);
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 2px;
  }
```

- [ ] **Step 4：驗證**

Run: `pnpm build` 後 `grep -c "ItemList" dist/education/index.html`
Expected: ≥ 1。常駐清單連結（非折疊）出現在 HTML。

- [ ] **Step 5：Commit**

```bash
git add src/pages/education/index.astro
git commit -m "feat(seo): 衛教列表頁 ItemList schema + 常駐可見文章清單"
```

---

### Task 16：assess.astro — SSR h1 + 引導文字

**Files:**
- Modify: `src/pages/assess.astro:16-17`

- [ ] **Step 1：在 `<main>` 內、island 之前加 SSR 可見 h1 與引導段**

把（行 16-17）：

```astro
    <main id="main-content" class="app-main" data-pagefind-body>
      <AssessmentShell client:load cards={approvedCards} />
```

改為：

```astro
    <main id="main-content" class="app-main" data-pagefind-body>
      <h1>兒童發展評估</h1>
      <p class="assess-intro">
        免費的兒童發展篩檢自評工具：依孩子年齡引導式作答，當下提供六大發展面向的風險分級與衛教建議。所有運算在瀏覽器完成，資料不上傳、免登入。
      </p>
      <AssessmentShell client:load cards={approvedCards} />
```

- [ ] **Step 2：在 `<style>` 加引導段樣式**

```css
  .assess-intro {
    font-size: var(--text-base);
    line-height: var(--lh-base);
    color: color-mix(in srgb, var(--text), var(--bg) 25%);
    max-width: 60ch;
    margin: 0 auto var(--space-5);
    text-align: center;
  }
```

> 註：assess 維持可索引（不傳 noindex），保留 `data-pagefind-body`。若 `AssessmentShell` 載入後自帶標題造成視覺重複，實作者可將此 h1 視覺降級（保持 SSR 可見即可），但**不得移除**——它是此可索引頁唯一的 h1 與爬蟲可見內容。

- [ ] **Step 3：驗證**

Run: `pnpm build` 後 `grep -c "<h1" dist/assess/index.html`
Expected: ≥ 1。

- [ ] **Step 4：Commit**

```bash
git add src/pages/assess.astro
git commit -m "feat(seo): assess 頁加 SSR h1 + 引導文字（可索引內容）"
```

---

## Phase 5：去舊定位（Footer / Header / manifest）

### Task 17：Footer.astro 吃 SITE，去 SMART on FHIR / 舊站名 / 死連結

**Files:**
- Modify: `src/components/blocks/Footer.astro:1-15`

- [ ] **Step 1：frontmatter 加 import**

```astro
---
import { SITE } from '../../lib/seo/site';
const year = 2026;
const version = 'v0.1.0';
---
```

- [ ] **Step 2：改 footer 內容（行 6-16）**

```astro
<footer class="site-footer">
  <div class="footer-inner container">
    <p class="copyright">&copy; {year} {SITE.organization.name}</p>
    <p class="meta">
      <span>{SITE.shortName} {version}</span>
      <span class="separator" aria-hidden="true">|</span>
      <a href={SITE.repo} class="footer-link" target="_blank" rel="noopener noreferrer">GitHub</a>
    </p>
  </div>
</footer>
```

（移除 `Powered by SMART on FHIR` span 與多餘 separator；`<style>` 不變。）

- [ ] **Step 3：驗證**

Run: `grep -nE "SMART on FHIR|CDSS|href=\"#\"" src/components/blocks/Footer.astro || echo "OK 已清除"`
Expected: `OK 已清除`。

- [ ] **Step 4：Commit**

```bash
git add src/components/blocks/Footer.astro
git commit -m "fix(seo): Footer 吃 SITE，移除 SMART on FHIR/舊站名/死連結"
```

---

### Task 18：Header.astro — history 移出主導覽 + site-title 吃 SITE

**Files:**
- Modify: `src/components/blocks/Header.astro:1-8,17`

- [ ] **Step 1：frontmatter import SITE 並移除 history navLink（行 1-8）**

```astro
---
import { SITE } from '../../lib/seo/site';

const currentPath = Astro.url.pathname;

const navLinks = [
  { label: '衛教資源', href: '/education/' },
  { label: '關於', href: '/about/' },
];

function isActive(href: string): boolean {
  return currentPath.startsWith(href);
}
---
```

- [ ] **Step 2：site-title 吃 SITE（行 17）**

```astro
    <a href="/" class="site-title">{SITE.name}</a>
```

> 此為 spec §5.15 標記「需 review gate 確認」項：history 自全域主導覽移除（家長仍可由評估結果脈絡進入 /history）。若使用者測試後希望保留 history 入口，改放 Footer 而非主導覽。`site-title` 若 `SITE.name` 過長擠壓 header，可改用 `{SITE.shortName}`。

- [ ] **Step 3：驗證**

Run: `pnpm check`
Expected: 無錯誤。

- [ ] **Step 4：Commit**

```bash
git add src/components/blocks/Header.astro
git commit -m "fix(seo): Header 移除 history 主導覽連結 + site-title 吃 SITE"
```

---

### Task 19：manifest 去舊品牌（template 佔位符 + build 注入）

**Files:**
- Modify: `scripts/templates/manifest.template.json:2-3`
- Modify: `scripts/build-manifest.mjs:4,13-15`

- [ ] **Step 1：template 改佔位符（加 description）**

```json
{
  "name": "__NAME__",
  "short_name": "__SHORT_NAME__",
  "description": "__DESCRIPTION__",
  "start_url": "__BASE_PATH__/",
  "scope": "__BASE_PATH__/",
  "display": "standalone",
  "theme_color": "__THEME_COLOR__",
  "background_color": "#ffffff",
  "icons": [
    { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable any" }
  ]
}
```

- [ ] **Step 2：build-manifest.mjs 注入品牌**

行 4 的 import 改為：

```js
import { BASE_PATH, THEME_COLOR, SITE_NAME, SITE_SHORT_NAME, SITE_DESCRIPTION } from './base.mjs';
```

行 13-15 的 `.replace` 鏈改為：

```js
const output = template
  .replace(/__BASE_PATH__/g, BASE_PATH)
  .replace(/__THEME_COLOR__/g, THEME_COLOR)
  .replace(/__NAME__/g, SITE_NAME)
  .replace(/__SHORT_NAME__/g, SITE_SHORT_NAME)
  .replace(/__DESCRIPTION__/g, SITE_DESCRIPTION);
```

- [ ] **Step 3：驗證產出**

Run: `pnpm build` 後 `grep -E "name|short_name" dist/manifest.json`
Expected: `"name": "Smart Pedi 兒童發展智慧評估"`、`"short_name": "Smart Pedi"`，無 `CDSA`。

- [ ] **Step 4：Commit**

```bash
git add scripts/templates/manifest.template.json scripts/build-manifest.mjs
git commit -m "fix(seo): manifest 去 CDSA 舊品牌，由 base.mjs 注入品牌字串"
```

---

## Phase 6：新頁面（404 / search）

### Task 20：自訂 404 頁

**Files:**
- Create: `src/pages/404.astro`

- [ ] **Step 1：建立 `src/pages/404.astro`**

```astro
---
import App from '../layouts/App.astro';
---

<App title="找不到頁面" description="您要找的頁面不存在。" noindex>
  <section class="notfound">
    <h1>找不到這個頁面</h1>
    <p>您要找的頁面可能已移除，或網址有誤。試試以下連結：</p>
    <ul>
      <li><a href="/">回首頁</a></li>
      <li><a href="/assess/">開始評估</a></li>
      <li><a href="/education/">瀏覽衛教內容</a></li>
    </ul>
  </section>
</App>

<style>
  .notfound {
    text-align: center;
    padding: var(--space-8) 0;
  }
  .notfound h1 {
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-4);
    color: var(--text);
  }
  .notfound p {
    font-size: var(--text-base);
    color: color-mix(in srgb, var(--text), var(--bg) 25%);
    margin-bottom: var(--space-5);
  }
  .notfound ul {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    align-items: center;
  }
  .notfound a {
    font-size: var(--text-lg);
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 2px;
  }
</style>
```

- [ ] **Step 2：驗證**

Run: `pnpm build && ls dist/404.html`
Expected: `dist/404.html` 存在（GitHub Pages 會以此作 404 回退）。

- [ ] **Step 3：Commit**

```bash
git add src/pages/404.astro
git commit -m "feat(seo): 自訂 404 頁（noindex + 導回連結）"
```

---

### Task 21：站內搜尋頁（Pagefind UI + SearchAction 目標）

**Files:**
- Create: `src/pages/search.astro`

- [ ] **Step 1：建立 `src/pages/search.astro`**

```astro
---
import App from '../layouts/App.astro';
import { BASE_PATH } from '../../scripts/base.mjs';
const base = BASE_PATH;
---

<App title="站內搜尋" description="搜尋兒童發展衛教內容。" noindex>
  <h1>站內搜尋</h1>
  <div id="search"></div>

  <link rel="stylesheet" href={`${base}/pagefind/pagefind-ui.css`} />
  <script is:inline src={`${base}/pagefind/pagefind-ui.js`}></script>
  <script is:inline>
    window.addEventListener('DOMContentLoaded', () => {
      // PagefindUI 於 build 後的 /pagefind/ 提供；dev 模式無索引屬正常。
      if (typeof window.PagefindUI !== 'function') return;
      const ui = new window.PagefindUI({ element: '#search', showSubResults: true });
      const q = new URLSearchParams(location.search).get('q');
      if (q) {
        const input = document.querySelector('#search input[type="text"]');
        if (input) {
          input.value = q;
          input.dispatchEvent(new Event('input', { bubbles: true }));
        }
      }
    });
  </script>
</App>

<style>
  #search {
    margin-top: var(--space-5);
  }
</style>
```

- [ ] **Step 2：驗證**

Run: `pnpm build && grep -c "pagefind-ui" dist/search/index.html`
Expected: ≥ 1（引用存在）。`/pagefind/` 由 postbuild 的 `pagefind --site dist` 產生，與 search 頁同在 dist。

- [ ] **Step 3：Commit**

```bash
git add src/pages/search.astro
git commit -m "feat(seo): 站內搜尋頁（Pagefind UI），對應 WebSite SearchAction"
```

---

## Phase 7：OG 圖生成（satori，見前置決策）

### Task 22：OG 圖生成腳本

**Files:**
- Create: `scripts/generate-og-image.mjs`
- Create: `public/og/og-default.png`（產出物，commit）
- Modify: `package.json`（scripts 加 `generate:og`）

- [ ] **Step 1：安裝依賴並確認字型格式**

```bash
pnpm add -D satori @resvg/resvg-js
ls node_modules/@fontsource/noto-sans-tc/files/ | grep -E "chinese-traditional-(400|700)-normal\.woff$"
```
Expected: 列出 `noto-sans-tc-chinese-traditional-400-normal.woff` 與 `...-700-normal.woff`。
（satori 支援 ttf/otf/**woff**，不支援 woff2；`@fontsource` 同時提供 `.woff`。若該套件僅有 woff2，改下載 Noto Sans TC TTF 並調整下方路徑。）

- [ ] **Step 2：建立 `scripts/generate-og-image.mjs`**

```js
import satori from 'satori';
import { Resvg } from '@resvg/resvg-js';
import { readFile, writeFile, mkdir } from 'fs/promises';
import { resolve, dirname } from 'path';
import { fileURLToPath } from 'url';
import { SITE_NAME, SITE_TAGLINE } from './base.mjs';

const __dirname = dirname(fileURLToPath(import.meta.url));
const repoRoot = resolve(__dirname, '..');
const fontDir = resolve(repoRoot, 'node_modules/@fontsource/noto-sans-tc/files');

const fontRegular = await readFile(resolve(fontDir, 'noto-sans-tc-chinese-traditional-400-normal.woff'));
const fontBold = await readFile(resolve(fontDir, 'noto-sans-tc-chinese-traditional-700-normal.woff'));

const svg = await satori(
  {
    type: 'div',
    props: {
      style: {
        width: '100%', height: '100%', display: 'flex', flexDirection: 'column',
        justifyContent: 'center', alignItems: 'flex-start',
        background: '#3d6b54', color: '#fbf8f2', padding: '90px',
        fontFamily: 'Noto Sans TC',
      },
      children: [
        { type: 'div', props: { style: { fontSize: 76, fontWeight: 700, marginBottom: 28, lineHeight: 1.2 }, children: SITE_NAME } },
        { type: 'div', props: { style: { fontSize: 38, opacity: 0.92, lineHeight: 1.4 }, children: SITE_TAGLINE } },
      ],
    },
  },
  {
    width: 1200, height: 630,
    fonts: [
      { name: 'Noto Sans TC', data: fontRegular, weight: 400, style: 'normal' },
      { name: 'Noto Sans TC', data: fontBold, weight: 700, style: 'normal' },
    ],
  },
);

const png = new Resvg(svg, { fitTo: { mode: 'width', value: 1200 } }).render().asPng();
await mkdir(resolve(repoRoot, 'public/og'), { recursive: true });
await writeFile(resolve(repoRoot, 'public/og/og-default.png'), png);
console.log('Built public/og/og-default.png (1200x630)');
```

- [ ] **Step 3：在 `package.json` scripts 加入（緊接 `generate:icons` 後）**

```json
    "generate:icons": "node scripts/generate-pwa-icons.mjs",
    "generate:og": "node scripts/generate-og-image.mjs",
```

- [ ] **Step 4：產生並驗證**

Run: `pnpm generate:og && file public/og/og-default.png`
Expected: `PNG image data, 1200 x 630`。

- [ ] **Step 5：Commit（含產出 png）**

```bash
git add scripts/generate-og-image.mjs public/og/og-default.png package.json pnpm-lock.yaml
git commit -m "feat(seo): OG 圖生成腳本（satori + Noto Sans TC）+ 產出 1200x630"
```

---

## Phase 8：守門測試

### Task 23：positioning.test.ts（原始碼字串守門）

**Files:**
- Create: `tests/seo/positioning.test.ts`

- [ ] **Step 1：建立測試**

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'fs';
import { resolve } from 'path';

const root = process.cwd(); // vitest 由 repo root 執行
const read = (p: string) => readFileSync(resolve(root, p), 'utf-8');

const BANNED_URL = [/yaocare/, /yao\.care\/smart-pedi-cds/];
const BANNED_POSITION = [/臨床決策/, /生命徵象/, /SMART on FHIR/];

describe('網址一致性', () => {
  const files = [
    'scripts/base.mjs',
    'src/lib/seo/site.ts',
    'src/pages/rss.xml.ts',
    'src/pages/llms.txt.ts',
    'scripts/templates/manifest.template.json',
    'src/data/site-faqs.ts',
  ];
  for (const f of files) {
    it(`${f} 無 yaocare / 舊子路徑`, () => {
      const c = read(f);
      for (const re of BANNED_URL) expect(c).not.toMatch(re);
    });
  }
  it('site.ts repo 指向 yao-care', () => {
    expect(read('src/lib/seo/site.ts')).toMatch(/github\.com\/yao-care\/smart-pedi-cds/);
  });
});

describe('去舊定位（隨每頁渲染或可索引頁）', () => {
  const files = [
    'src/components/blocks/Footer.astro',
    'src/pages/index.astro',
    'src/pages/about.astro',
    'src/pages/rss.xml.ts',
    'scripts/templates/manifest.template.json',
    'scripts/base.mjs',
    'src/lib/seo/site.ts',
  ];
  for (const f of files) {
    it(`${f} 無舊定位字串`, () => {
      const c = read(f);
      for (const re of BANNED_POSITION) expect(c).not.toMatch(re);
    });
  }
});

describe('manifest 品牌一致', () => {
  it('template 用佔位符、無 CDSA', () => {
    const c = read('scripts/templates/manifest.template.json');
    expect(c).toMatch(/__NAME__/);
    expect(c).not.toMatch(/CDSA/);
  });
});
```

- [ ] **Step 2：跑測試**

Run: `pnpm test -- tests/seo/positioning.test.ts`
Expected: PASS（前面 Phase 1-7 的去定位改動已完成；若有殘留字串會在此被抓出並修正）。

- [ ] **Step 3：Commit**

```bash
git add tests/seo/positioning.test.ts
git commit -m "test(seo): positioning 守門（網址一致 + 去舊定位 + manifest 品牌）"
```

---

### Task 24：verify-seo.mjs（建置後 dist 守門）+ 接上 postbuild

**Files:**
- Create: `scripts/verify-seo.mjs`
- Modify: `package.json:24`（postbuild）

- [ ] **Step 1：建立 `scripts/verify-seo.mjs`**

```js
import { readFile, access, readdir } from 'fs/promises';
import { resolve } from 'path';

const dist = resolve(process.cwd(), 'dist');
let failed = false;
const ok = (m) => console.log('✓', m);
const fail = (m) => { console.error('✗', m); failed = true; };
const exists = (p) => access(resolve(dist, p)).then(() => true).catch(() => false);

// 1. robots.txt 含 Sitemap
const robots = await readFile(resolve(dist, 'robots.txt'), 'utf-8').catch(() => '');
robots.includes('Sitemap:') ? ok('robots.txt 含 Sitemap') : fail('robots.txt 缺 Sitemap 行');

// 2. llms.txt 網域正確、無 yaocare
const llms = await readFile(resolve(dist, 'llms.txt'), 'utf-8').catch(() => '');
(llms.includes('smart-pedi-cds.yao.care') && !llms.includes('yaocare'))
  ? ok('llms.txt 網域正確') : fail('llms.txt 網域錯誤或含 yaocare');

// 3. 404.html 存在
(await exists('404.html')) ? ok('404.html 存在') : fail('404.html 缺');

// 4. sitemap 不含 noindex 路徑
const sm = await readFile(resolve(dist, 'sitemap-0.xml'), 'utf-8').catch(() => '');
/\/(settings|admin|result|workspace|history|search)\//.test(sm)
  ? fail('sitemap 含 noindex 路徑') : ok('sitemap 無 noindex 路徑');

// 5. 每篇衛教文章有 h1
const eduDir = resolve(dist, 'education');
const entries = await readdir(eduDir, { withFileTypes: true }).catch(() => []);
let missingH1 = 0;
for (const e of entries) {
  if (!e.isDirectory()) continue;
  const html = await readFile(resolve(eduDir, e.name, 'index.html'), 'utf-8').catch(() => '');
  if (html && !html.includes('<h1')) missingH1++;
}
missingH1 === 0 ? ok('衛教文章皆有 h1') : fail(`${missingH1} 篇衛教文章缺 h1`);

// 6. noindex 頁不進 Pagefind（settings 為代表）
const settings = await readFile(resolve(dist, 'settings/index.html'), 'utf-8').catch(() => '');
settings && settings.includes('data-pagefind-body')
  ? fail('settings 仍含 data-pagefind-body') : ok('settings 已排除 Pagefind');

if (failed) {
  console.error('\nSEO 建置後守門失敗。');
  process.exit(1);
}
console.log('\nSEO 建置後守門全數通過。');
```

- [ ] **Step 2：接上 postbuild（行 24）**

```json
    "postbuild": "pagefind --site dist && node scripts/build-sw.mjs && node scripts/build-manifest.mjs && node scripts/verify-seo.mjs",
```

- [ ] **Step 3：完整 build 驗證**

Run: `pnpm build`
Expected: postbuild 末尾印出「SEO 建置後守門全數通過。」，exit 0。

- [ ] **Step 4：Commit**

```bash
git add scripts/verify-seo.mjs package.json
git commit -m "test(seo): verify-seo 建置後守門（robots/llms/sitemap/404/h1/pagefind）接上 postbuild"
```

---

## 最終驗證

- [ ] **全量測試 + build**

Run: `pnpm check && pnpm lint && pnpm test && pnpm build`
Expected: 全綠；postbuild 守門通過。

- [ ] **人工抽查（Rich Results）**

Build 後將 `dist/index.html`、任一 `dist/education/*/index.html` 的 JSON-LD 貼到 Google Rich Results Test，確認 Organization/WebSite/SoftwareApplication/FAQPage/MedicalWebPage 無錯誤。

---

## Self-Review（計畫對照 spec）

**1. Spec coverage：**
- §4.1 base.mjs+site.ts → Task 1 ✓；§4.2 schema → Task 2 ✓；§4.3 JsonLd → Task 3 ✓；§4.4 robots → Task 7 ✓；§4.5 llms → Task 8 ✓；§4.6 og → Task 22 ✓；§4.7 404 → Task 20 ✓；§4.8 search → Task 21 ✓。
- §5.1 Base → Task 4 ✓；§5.2 index → Task 12 ✓；§5.3 about → Task 13 ✓；§5.4 文章頁 → Task 14 ✓；§5.5 列表頁 → Task 15 ✓；§5.6 assess → Task 16 ✓；§5.7 site-faqs → Task 11 ✓；§5.8 Breadcrumb → Task 3 ✓；§5.9 rss → Task 9 ✓；§5.10 Footer → Task 17 ✓；§5.11 sitemap → Task 10 ✓；§5.12 Pagefind → Task 5/6 ✓；§5.13 noindex → Task 4/5/6 ✓；§5.14 manifest → Task 19 ✓；§5.15 Header → Task 18 ✓。
- §7 測試：schema → Task 2、positioning → Task 23、h1/pagefind/建置後 → Task 24 ✓。

**2. Placeholder scan：** 無 TBD/TODO；OG 字型路徑於 Task 22 Step 1 以 `ls` 驗證並給 fallback；sitemap lastmod 明確標為選用（filter 為必做）。

**3. Type consistency：** schema 工廠名稱（`organizationSchema`/`webSiteSchema`/`softwareApplicationSchema`/`medicalWebPageSchema`/`articleListSchema`/`breadcrumbSchema`/`faqPageSchema`）於 Task 2 定義，Task 3/12/13/14/15 引用一致；`SITE` 欄位（name/shortName/tagline/description/organization/repo/ogImagePath/logoPath/sameAs）於 Task 1 定義，後續引用一致；`breadcrumbSchema(site, items)`、`medicalWebPageSchema(site, {...})` 簽名與 Task 2 測試一致。

**4. 已知風險落點：** OG 中文字型（Task 22，前置決策可改純拉丁）；search 頁 dev 模式無索引（build 後正常）；Header history 移出為 review-gate 項（Task 18 註明）。
