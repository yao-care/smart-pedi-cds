# 收尾清單 — 清空所有尾巴（2026-07-08）

/clear 後**一次全部執行**用。每項的判斷已拍板（見「決策」），下次為**純執行**。
每項附：檔案、做法、驗收。做完逐項打勾，最後整批 commit + push。

前置事實（2026-07-08）：`main` 與 `origin/main` 同步（`864b7e4`）；B1/B2(釘版)/B3
已上線；單元 66 檔 710 綠、`pnpm check` 0 error。本清單 = 剩餘全部尾巴。

決策（用戶 2026-07-08 拍板）：dead code **刪除**；MediaPipe **self-host**；舊截圖
**gitignore**；真機/外部類 **盡量自動化，殘餘才交用戶**；B1(a) 臨床 ONNX **延後**。

---

## Group A — 工程（可一次做完，無需硬體/外部）

### A1. 刪除 dead code `analyzeAssessment`
- **檔案**：`src/engine/cdsa/assessment-analyzer.ts`、`tests/engine/assessment-analyzer.test.ts`
- **事實**：`analyzeAssessment` 無真實 caller（唯一出現在 `active-module-analysis.ts:11`
  是**註解**，非 import）；真實路徑是 partialAnalysis。留著是陷阱（未來 ResultView
  fallback 若餵非 undefined 物件會重演 drawing「細動作 15 分」bug）。
- **做法**：
  1. 先 `grep -rn "AssessmentAnalysisResult" src/ tests/` 確認 interface 無他處 import
     （只被本檔用 → 整檔刪；若他處有用 → 保留 interface、只刪函式）。
  2. 刪 `analyzeAssessment` 函式（或整檔）+ 刪 `tests/engine/assessment-analyzer.test.ts`。
  3. 改寫 `active-module-analysis.ts:11` 註解（移除「接在 dead code」的過時描述）。
  4. 更新 memory `triage-data-flow.md`：dead code 已移除。
- **驗收**：`pnpm check` 0 error；`pnpm test` 綠；`git grep analyzeAssessment src/` 無殘留。

### A2. B2 self-host MediaPipe（去除 Google Storage + jsdelivr 外部依賴）
- **檔案**：`public/models/`、`src/engine/cdsa/gross-motor-analysis.ts`、
  `tests/engine/gross-motor-cdn-pinning.test.ts`、新 `scripts/copy-mediapipe-assets.mjs`
- **事實**：`public/models/` 已有本地 ONNX（`drawing-classifier.onnx`/`risk-model.onnx`）
  = self-host 有先例。**npm 包 `node_modules/@mediapipe/tasks-vision/wasm/` 已自帶**
  wasm（`vision_wasm_internal.{js,wasm}` 等）→ WASM 免抓網路，直接複製。
- **做法**：
  1. Pose 模型：`curl -o public/models/pose_landmarker_lite.task \
     "https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_lite/float16/1/pose_landmarker_lite.task"`
     （~3MB；HEAD 200 已驗）。
  2. WASM：寫 `scripts/copy-mediapipe-assets.mjs` 把
     `node_modules/@mediapipe/tasks-vision/wasm/` 複製到 `public/models/mediapipe-wasm/`；
     掛到 `predev`/`prebuild`（與既有 build-content-index 並列），避免手動漏複製。
  3. URL 常數改指本站（注意 `BASE_PATH` 自訂域為空字串，從 `scripts/base.mjs` 匯入，
     **勿在 frontmatter 直接讀 `import.meta.env.BASE_URL`**——見 CLAUDE.md esbuild 陷阱）：
     - `MEDIAPIPE_WASM_URL` → `${BASE_PATH}/models/mediapipe-wasm`
     - `POSE_LANDMARKER_MODEL_URL` → `${BASE_PATH}/models/pose_landmarker_lite.task`
  4. 更新守門測試：改斷言 URL **不含外部 host**（no `storage.googleapis.com`、no
     `jsdelivr`、no `@latest`/`/latest/`），改為以 `/models/` 開頭。
  5. 確認 `.task` 不被 git-lfs / .gitignore 擋（~3MB 直接進 repo 可接受，同既有 onnx）。
- **驗收**：`pnpm build` 後 `dist/models/pose_landmarker_lite.task` + `dist/models/mediapipe-wasm/`
  存在；守門測試綠；`pnpm check` 0 error；部署後 live `/models/pose_landmarker_lite.task` HEAD 200。

### A3. 模組 addAnalysis 稽核 + 收斂（關掉 TODO.md「可選 followup」）
- **檔案**：`src/components/assess/{DrawingModule,VoiceModule,VideoModule,GameModule,QuestionnaireModule}.svelte`、
  `src/components/assess/ResultView.svelte`、`src/lib/assessment/active-module-analysis.ts`
- **事實**：目前 voice→VoiceModule inline、gross-motor→ResultView 背景 enrich、
  drawing→DrawingModule 已接；VideoModule 未直接呼叫（gross-motor 在 ResultView）。
  triage 有 guard（`voiceDurationTotal>0`/`if(input.grossMotor)`/`shapes.length>0`）→
  無 symptom，但接線分散易重演漏接 bug。
- **做法**：稽核五模組各在何處呼叫 `addAnalysis`（或為何不需要），把結論寫成
  `active-module-analysis.ts` 頂註 + 一張對照表。若判斷應統一，把 VideoModule 的
  gross-motor 觸發也收斂進 helper（與 voice 同模式），移除 ResultView 特例。
  （若判斷維持現狀最穩，明確記錄「by-design：重 ML 留 ResultView 背景」並關掉此項。）
- **驗收**：一份對照結論（模組→呼叫點→guard），無 fallback 假資料風險；相關測試綠。

### A4. B3 匯出自動化 smoke（取代手動）
- **檔案**：`tests/e2e/detection-coverage.spec.ts`（或新 `tests/e2e/history-export.spec.ts`）、
  沿用 `tests/e2e/helpers/export-inspector.ts`
- **做法**：完成一次評估 seed IDB → 導到 `/history/` → 點「⤓ 匯出資料（JSON）」→
  用 Playwright `page.waitForEvent('download')` 攔截 → 斷言 filename 符
  `smart-pedi-history-\d{4}-\d{2}-\d{2}.json`、內容 JSON parse 後有 `format:'smart-pedi-history'`
  / `version:1` / `children[].assessments`。
- **測法**：`PLAYWRIGHT_BASE_URL=https://smart-pedi-cds.yao.care pnpm exec playwright test
  history-export --workers=1`（部署後跑；或對 preview build）。
- **驗收**：E2E 綠。**這一項綠 = B3 完全關閉，無殘留手動 smoke**。

### A5. 舊截圖 gitignore
- **檔案**：`.gitignore`、`docs/superpowers/specs/screenshots/`（未追蹤）
- **做法**：加一行 `docs/superpowers/specs/screenshots/` 進 `.gitignore`。
- **驗收**：`git status --short` 不再顯示該目錄。

---

## Group B — 盡量自動化（真機/外部；殘餘才交用戶）

### B1. A1 gross_motor 盡量自動化 + 殘餘真機目視
- **檔案**：`tests/e2e/detection-coverage.spec.ts`（維度②已有 gross_motor fake-video soft annotation）
- **做法**：把 soft annotation 強化為**硬覆蓋暖啟落地邏輯**——E2E 用 fake video 跑
  `warmUpGrossMotor()` → `extractPosesFromVideo` → 斷言暖啟後 frames>0 或 15s timeout
  內回非 null（驗證預熱 + timeout 邏輯，不依賴真實 GPU 時序）。
- **殘餘（無法自動化，交用戶 <5 分）**：真實裝置（硬體 GPU、真鏡頭）首次評估一次，
  目視確認 triage 出現 `gross_motor/poseClassification` detail、結果頁 spinner <3s。
  不過則走計畫後備（預熱提前 / async enrich / 調 timeout）。
- **驗收**：E2E 覆蓋暖啟邏輯綠；殘餘真機目視項明列於此，用戶跑一次即關閉。

### B2. ISMS 端登錄文字產出（外部系統，交用戶貼上）
- **檔案**：新 `docs/security/isms-registry-entries.md`（本 repo 內產文字，貼到 ISMS 系統）
- **事實**：ISO 27001 A.5.24/A.5.26/A.5.29/A.8.26 文件+證據已在本 repo（見
  `security-isms-governance.md`）；剩「ISMS 端登錄路徑」不在本 repo。
- **做法**：產出可直接貼上的登錄條目（每控制項 → 本 repo 證據路徑 + 掃描
  `20260610-044840-127e` 對應）。
- **殘餘（交用戶）**：用戶把該文字登錄進 ISMS 系統。
- **驗收**：`isms-registry-entries.md` 完成；用戶貼上即關閉。

---

## Group C — 明確延後（清空、不再懸）

### C1. B1(a) 臨床 ONNX 粗大動作模型 — 延後，非阻塞
- **理由**：B1 已改 display-only，**稀釋安全風險已消除**；pose 目前 display-only 不 gate。
  臨床 ONNX 需標註臨床資料集 + 驗證，屬獨立研究工作，非本收尾範圍。
- **狀態**：明確**延後**。pose 維持 display-only。未來若要做，另開 spec。
  （此項寫在此僅為「不再懸」，執行時**不動作**，確認即可。）

---

## 收尾動作

1. Group A 全做完 → `pnpm check` + `pnpm test` + `pnpm build` 全綠。
2. Group B 自動化部分做完；殘餘（A1 真機、ISMS 貼上）明確交用戶。
3. 更新 memory（`detection-coverage-e2e.md`、`TODO.md`、`triage-data-flow.md`）標記全關。
4. 整批 commit（分主題）→ push main（**對外部署，push 前照例確認**）。
5. 部署後跑 A4/B1 的 live E2E；用戶做 A1 真機目視 + ISMS 貼上 → **全清空**。
